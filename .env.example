# Copia este archivo a .env (o exporta la variable en tu shell) y rellena la clave.
# El .env real está en .gitignore: NUNCA se commitea.

# API key de PixelLab (MCP de generación de pixel-art).
# La consume .mcp.json → servidor "pixellab" vía ${PIXELLAB_API_KEY}.
PIXELLAB_API_KEY=
